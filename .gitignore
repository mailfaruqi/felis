node_modules
dist
.env
.DS_Store
*.local

# Raw GBIF pulls are large and reproducible via `bun run data:fetch`.
data/raw/
data/processed/

# public/data/ IS committed: it is the deployable dataset, and regenerating it
# takes about an hour, so a clone or a deploy would otherwise have no map.
