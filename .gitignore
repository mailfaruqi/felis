node_modules
dist
.env
.DS_Store
*.local

# Raw GBIF pulls are large and fully reproducible via `bun run data:fetch`.
data/raw/

# Processed output is generated by `bun run data:process` and copied into
# public/data/ for serving. Regenerate rather than commit.
data/processed/
public/data/
